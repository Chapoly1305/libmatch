# Core dependency - angr binary analysis framework
angr>=9.2.192

# Graph analysis for call graphs
networkx>=3.0

# YAML output format
PyYAML>=6.0

# ELF file creation
makeelf>=0.3.0

# Colored terminal output
clint>=0.5.1

# Fast Levenshtein distance computation (optional but highly recommended for performance)
# Provides 2-5x speedup for block similarity calculations
rapidfuzz>=3.0.0

# NumPy/SciPy for vectorized distance computations (optional but highly recommended)
# Provides 10-50x speedup for block matching in functions with 50+ blocks
numpy>=1.24.0
scipy>=1.10.0
